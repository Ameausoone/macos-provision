#!/usr/bin/env zsh

# add pip
# I'm not using python through asdf due to a bug during python installation, I had to back to brew.
# export PIP_DIR="$(asdf where python)/bin"
# export PATH="${PIP_DIR}:${PATH}"
